<Window x:Class="GestaoProdutos.UI.Views.ProdutoFormAdd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GestaoProdutos.UI.Views"
        xmlns:behaviors="clr-namespace:GestaoProdutos.UI.Behaviors"
        Title="Adicionar / Editar Produto"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Height="270" Width="400">


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,5">
            <Label Content="Nome" Width="90" VerticalAlignment="Center"/>
            <TextBox Width="289" Text="{Binding Nome}" FontSize="14"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5">
            <Label Content="Descrição" Width="90" VerticalAlignment="Center"/>
            <TextBox Width="289" Text="{Binding Descricao}" FontSize="14"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,5">
            <Label Content="Quantidade" Width="90" VerticalAlignment="Center"/>
            <TextBox Width="290" Text="{Binding Quant, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     behaviors:TextBoxBehaviors.OnlyNumbers="True" FontSize="14"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,5">
            <Label Content="Preço" Width="90" VerticalAlignment="Center"/>
            <TextBox Width="290" behaviors:TextBoxBehaviors.OnlyNumbers="True"
                     Text="{Binding Preco, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,  ConverterCulture=pt-BR, StringFormat=C}" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="105,15,0,0" HorizontalAlignment="Right" Width="170">
            <Button Content="OK" Width="80" Margin="5,0" Command="{Binding OkProdutoCommand}"
                    Foreground="Green" FontWeight="Bold"/>

            <Button Content="SAIR" Width="80"
                    Foreground="DarkRed" 
                    Command="{Binding ClickBtnSairCommand}"
                    FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>